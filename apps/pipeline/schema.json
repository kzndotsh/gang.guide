{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Gang Organization",
  "description": "Schema for a gang.guide org file in data/orgs/. Lint required fields are id, name, description, sources. See docs/SCHEMA.md.",
  "type": "object",
  "required": ["id", "name", "description", "sources"],
  "properties": {
    "id": {
      "type": "string",
      "pattern": "^org:[a-z0-9]+(?:-[a-z0-9]+)*$",
      "description": "Unique identifier in format org:slug-name"
    },
    "name": {
      "type": "string",
      "minLength": 2,
      "description": "Display name of the organization"
    },
    "aliases": {
      "type": "array",
      "items": { "type": "string", "maxLength": 60 },
      "default": [],
      "description": "Alternative names, abbreviations"
    },
    "type": {
      "type": "string",
      "enum": ["street_gang", "organized_crime", "motorcycle_club", "prison_gang", "white_supremacist", "alliance", "nation"],
      "description": "Organization type"
    },
    "lane": {
      "type": "string",
      "description": "Lane ID for timeline positioning (must match lanes.json)"
    },
    "metro": {
      "type": "string",
      "description": "Primary city or area"
    },
    "description": {
      "type": "string",
      "minLength": 50,
      "maxLength": 800,
      "description": "Factual 2-4 sentence description starting with the canonical name"
    },
    "founded_year": {
      "type": "integer",
      "minimum": 1800,
      "maximum": 2026,
      "description": "Year the organization was founded"
    },
    "founded_year_precision": {
      "type": "string",
      "enum": ["exact", "circa", "decade", "estimate"],
      "description": "How precise the founding year is"
    },
    "colors": {
      "type": "array",
      "items": { "type": "string" },
      "default": [],
      "description": "Gang colors"
    },
    "symbols": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Identifiers; title case except ALL-CAPS abbreviations \u22646 chars"
    },
    "military_service": {
      "type": "string",
      "description": "Optional. Military branches associated with an MC or similar org"
    },
    "nation_affiliation": {
      "type": ["string", "null"],
      "pattern": "^org:[a-z0-9]+(?:-[a-z0-9]+)*$",
      "description": "Parent nation org ID (e.g. org:crips, org:folk-nation)"
    },
    "status": {
      "type": "string",
      "enum": ["active", "inactive", "unknown"],
      "description": "Whether the organization is currently operating"
    },
    "disbanded_year": {
      "type": ["integer", "null"],
      "minimum": 1800,
      "maximum": 2026,
      "description": "Year the organization disbanded (if inactive)"
    },
    "membership_estimate": {
      "type": ["integer", "null"],
      "minimum": 1,
      "description": "Estimated membership count"
    },
    "sources": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["url", "title"],
        "properties": {
          "url": { "type": "string", "format": "uri" },
          "title": { "type": "string", "minLength": 1 }
        }
      },
      "description": "Source references with URL and title"
    }
  },
  "additionalProperties": false
}
