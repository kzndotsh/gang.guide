# Pipeline dependencies
#
# Install:
#   pip install -e "apps/pipeline[scrape,extract]"     # full pipeline
#   pip install -e "apps/pipeline[scrape]"             # scraping only (no LLM costs)
#   pip install -e "apps/pipeline[dev]"                # dev tools (ruff, pytest)
